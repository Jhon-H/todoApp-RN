import { StyleSheet } from 'react-native';
import { variablesStyle } from './variables';

export const styles = StyleSheet.create({
  globalMargin: {
    marginHorizontal: variablesStyle.globalMarginHorizontal,
    taskCardMarginHorizontal: 10

  }
})
